const fs = require('fs')
const ints = fs.readFileSync('integers.txt', 'utf-8').split('\n');

const sortAndCount = (arr1, arr2) => {
  let sorted = [];
  while (arr1.length && arr2.length) {
    
  }
}
const inversions = arr => {

}

module.exports = inversions;